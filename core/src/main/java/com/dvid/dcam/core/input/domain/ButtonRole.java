package com.dvid.dcam.core.input.domain;

/** Application meaning assigned to a physical device control. */
public enum ButtonRole {
    RECORD,
    IMPORTANT_RECORDING,
    PHOTO_CAPTURE,
    AUDIO_CAPTURE,
    PTT,
    SOS
}
